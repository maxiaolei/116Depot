<%= form_for(@star_rate) do |f| %>
  <% if @star_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@star_rate.errors.count, "error") %> prohibited this star_rate from being saved:</h2>

      <ul>
      <% @star_rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
